// Single place controlling "today" for the prototype, so seeded history and newly-created
// orders agree on what "today" means without depending on the real wall-clock date.
export const TODAY = '2026-08-25';
